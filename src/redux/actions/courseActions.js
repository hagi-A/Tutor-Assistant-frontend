// courseActions.js

// Action to set the course code
export const setCourseCode = (code) => ({
  type: "SET_COURSE_CODE",
  payload: code,
});

// Action to set the course title
export const setCourseTitle = (title) => ({
  type: "SET_COURSE_TITLE",
  payload: title,
});

// Other actions related to courses can be added here as needed...
